# RAG 分块

## 固定大小分块
固定大小分块是最简单的分块方式，主要将文本按照固定的文本大小或者token数对文本进行切分。

主要的优点是处理简单，且由于所有分块长度一致，便于批量处理。


## 字符递归分块
首先，按文档内的天然分隔符（如段落、章节）进行初步分块。

接下来，如果大小超过预定义的数据块大小限制，则将每个数据块拆分为较小的数据块。但是，如果 chunk 符合 chunk-size 限制，则不会进行进一步的拆分。


## 按照文档结构分块

利用文档中已有的结构信息（如标题、章节、段落）来设定分块边界。

这样可保持文档的结构完整性，并对齐逻辑结构。