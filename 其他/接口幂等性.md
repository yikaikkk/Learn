# 接口幂等

## 含义
幂等是一个数学概念，指多次执行结果一致

## 在开发中一些使用到的幂等解决方案

### 前端限制

在前端加入防抖等功能，防止用户重复访问


### 后端限制

 加锁+唯一校验

通常使用分布式锁，在之前的一个项目中，在插入绑定关系时，如果并发过高，虽然已经使用了数据库中的数据的唯一性校验，但是也会出现插入两条的情况。为了防止这种情况，加入了一个分布式锁，每次插入时，创建一个分布式锁，防止重复插入。


### 数据库限制
 唯一索引

在数据库中创建表的时，创建一个唯一索引，防止表中数据的重复插入
